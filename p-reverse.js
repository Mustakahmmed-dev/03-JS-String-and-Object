// reverse an array
let numbers = [20, 30, 50, 56, 80, 90, 73];
console.log(numbers);

numbers.reverse();
console.log("Reverse valuess: ", numbers);

// Reverse numbers with for of loop

let numbers2 = [1, 2, 3, 4, 5, 6, 7, 8, 9];

let revNumbers = [];
for(let num of numbers2){
    // console.log(num);
    revNumbers.push[num];
}
console.log(revNumbers);